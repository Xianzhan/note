<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xianzhan.note.data.mybatis.mapper.BaseMapper">

    <select id="selectBase" resultType="xianzhan.note.data.mybatis.entity.Base">
        select * from base where id = #{id}
    </select>

</mapper>